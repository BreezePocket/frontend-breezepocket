# Cloudflare Pages project config (static site, free tier).
# Deploy with: npm run deploy   (requires `npx wrangler login` or CLOUDFLARE_API_TOKEN)
name = "breezepocket"
pages_build_output_dir = "dist"
compatibility_date = "2026-09-08"
